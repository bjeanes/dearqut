- form_for(@message, :html => { :id => "msgform"}) do |f|
  = f.error_messages
  %h2.center Why do you think this message is inappropriate?
  #message-body= f.text_area :flag_reason
  %br
  %span.right
    = f.submit 'SUBMIT', :class => 'button inner'