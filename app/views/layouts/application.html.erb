<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
<head>
	<meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>Dear QUT</title>
  <%= stylesheet_link_tag 'application' %>
  <%= javascript_include_tag 'http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.min.js', 'application' %>
</head>
	<body>
		<div id="container">
		  <div id="menu">
		  <% tabs do |tab| %>
  		    <%= tab.home "Home", root_path %>
  		    <% if logged_in? %>
  		      <%= tab.logout "Log out", logout_path %>
  		    <% else %>
  		      <%= tab.login "Log in", login_path %>
  		      <%= tab.signup "Sign up", signup_path %>
  		      <%= tab.about "About", root_path %>
  		      <%= tab.browse "Browse", messages_path %>
  		      <%= tab.contact "Contact", root_path %>
  		    <% end %>
		    <% end %>
		  </div>
			<h1 id="title"><a href="/">Dear QUT,</a></h1>
			<div id="content">
			<p style="color: green"><%= flash[:notice] %></p>

			<%= yield %>
			</div>
			<div id="footer">
				<ul>
					<li class="first">Sync Your <span class="large">Twitter</span>
					<li class="second">Need Some <span class="huge">Help</span>
					<li class="third"><span class="larger">Terms</span> and <span class="larger">Conditions</span>
				</ul>
			</div>
		</div>
	</body>
</html>